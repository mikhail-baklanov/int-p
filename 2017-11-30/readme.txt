Задача: Есть плагин под Maven, автоматически проверяющий стиль написания кода (checkStyle). Плагин запускается перед этапом компиляции. Для плагина можно описывать исключения и хранить их в отдельном файле (файл suppressions.xml).
Есть папка с проектом, в ней может быть много дополнительных папок, с некоторого уровня начинается пакет (ru/intrust/client...), в конце пути лежат файлы классов. Есть файл исключений. Его формат:
…<suppres files=”ru[\\/]intrust[\\/] … abc.java ” check …> (нас интересуют такие строки, в файле могут содержаться строки и другого типа)
Файл представлен в кодировке ASCII.
(Нужно использовать регулярные выражения)
(В задании допускается построение полного пути к файлу. Для определения наличия\отсутствия файла в проекте нужно сравнить конец пути к файлу с путем в файле suppressions.xml)
Задача: нужно определить, какие строки в файле лишние, т.е. каких файлов уже в проекте нет.